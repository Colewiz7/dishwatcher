services:
  dishwatcher:
    build: .
    container_name: dishwatcher
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./static:/app/static
    environment:
      - DISH_API_KEY=changeme
      - SAVE_DIR=/app/data
      - CAMERA_ROTATION=180
      # dashboard password (leave empty to disable)
      - ADMIN_PASSWORD=
      # ssim detection
      - SSIM_THRESHOLD=0.82
      - YOLO_ENABLED=true
      - YOLO_MODEL_PATH=/app/yolov8n.pt
      - CONFIDENCE_THRESHOLD=0.40
      # counter detection (set to true + define counter roi via dashboard)
      - COUNTER_ENABLED=false
      # state machine
      - CONSENSUS_WINDOW=7
      - CONSENSUS_THRESHOLD=5
      - GRACE_MINUTES=90
      - DB_PATH=/app/data/dishwatcher.db
      # discord (optional)
      # - DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
      # - DISCORD_MENTION=<@your_user_id>

    # reverse proxy (uncomment + edit for your domain)
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.http.routers.dishwatcher.rule=Host(`dishes.yourdomain.com`)"
    #   - "traefik.http.routers.dishwatcher.entrypoints=websecure"
    #   - "traefik.http.routers.dishwatcher.tls.certresolver=letsencrypt"
    #   - "traefik.http.services.dishwatcher.loadbalancer.server.port=8000"
